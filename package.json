{
  "name": "theblogger",
  "version": "1.0.0",
  "description": "A simple blogging platform with user auth, posts, and comments",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "cross-env NODE_ENV=production node backend/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
